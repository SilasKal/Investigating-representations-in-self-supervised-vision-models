# MAPS

This is the repository of MAPS (Manifolds of Artificial Parametric Scenes).

## Installation

To install maps run:

```bash
conda env create -f environment.yml
```

then:

```bash
pip install -e
```
